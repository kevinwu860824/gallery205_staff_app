import 'package:flutter/material.dart';

class CheckCashOpenScreen extends StatelessWidget {
  const CheckCashOpenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("錢櫃管理")),
      body: const Center(child: Text("功能開發中")),
    );
  }
}